import 'package:flutter/material.dart';

class AttendancScreen extends StatelessWidget {
  const AttendancScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}